import user from '../modules/user/store';
// import decoration from '../modules/decoration/store';
import admin from '../modules/admin/store';
import seller from '../modules/seller/store';
import store from '../modules/store/store';

export default {
    state: {
    },
    getters: {
    },
    actions: {
    },
    modules: {
        user,
        // decoration,
        // admin,
        seller,
        store
    }
};
